<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:tb="http://www.hardcodet.net/taskbar"
					xmlns:lex="http://wpflocalizeextension.codeplex.com"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<tb:TaskbarIcon x:Key="NotifyIcon" DoubleClickCommand="{Binding ShowCommand}" DataContext="{Binding Source={StaticResource Locator}, Path=NotifyIcon}"
					IconSource="/Blitzy;component/Resources/AppIcon.ico" ToolTipText="{Binding Title}">
		<tb:TaskbarIcon.ContextMenu>
			<ContextMenu>
				<MenuItem Header="{lex:Loc Blitzy.Resources.ShowWindow}" Command="{Binding ShowCommand}" />
				<MenuItem Header="{lex:Loc Blitzy.Resources.Settings}" Command="{Binding SettingsCommand}" />
				<MenuItem Header="{lex:Loc Blitzy.Resources.Quit}" Command="{Binding QuitCommand}" />
			</ContextMenu>
		</tb:TaskbarIcon.ContextMenu>
	</tb:TaskbarIcon>
</ResourceDictionary>