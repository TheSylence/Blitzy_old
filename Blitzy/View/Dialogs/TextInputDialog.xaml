<v:CloseableView x:Class="Blitzy.View.Dialogs.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:lex="http://wpflocalizeextension.codeplex.com"
		xmlns:v="clr-namespace:Blitzy.View"
		DataContext="{Binding Source={StaticResource Locator}, Path=TextInputDialog}" FocusManager.FocusedElement="{Binding ElementName=txtInput}"
        Title="{Binding Caption}" Height="125" Width="300" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="ToolWindow" ResizeMode="NoResize">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBlock Text="{Binding LabelText}" Margin="5" />
		<TextBox Grid.Row="1" Name="txtInput" Text="{Binding Input, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="Ok" IsDefault="True" Command="{Binding OkCommand}" />
			<Button Content="Cancel" IsCancel="True" />
		</StackPanel>
	</Grid>
</v:CloseableView>